version: '3'

services:
  database:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=carlos
      - POSTGRES_PASSWORD=carlos
      - POSTGRES_DB=carinvoces